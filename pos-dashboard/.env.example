# Base URL API dashboard.
# Local dev (with Vite proxy):
VITE_API_BASE_URL=/api

# Production / public backend example:
# VITE_API_BASE_URL=https://your-domain.com/api

# Optional: whitelist host dev server. Kosong = allow all host.
# VITE_ALLOWED_HOSTS=localhost,.ngrok-free.dev
